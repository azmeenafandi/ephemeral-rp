# .understandignore — patterns for files/dirs to exclude from analysis
# Syntax: same as .gitignore (globs, # comments, ! negation, trailing / for dirs)
#
# Built-in defaults (always excluded unless negated):
#   node_modules/, .git/, dist/, build/, obj/, *.lock, *.min.js, images, fonts, etc.

# --- This tool's own output (generated, not source) ---
.understand-anything/

# --- Build artifacts (generated, not authored) ---
tsconfig.node.tsbuildinfo
tsconfig.tsbuildinfo
vite.config.d.ts
vite.config.js
worker/dist-worker.js

# --- Gitignored files to also skip during analysis ---
.wrangler/
.dev.vars*
.env*
